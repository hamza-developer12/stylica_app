<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color -->
    <solid android:color="@color/primary_light_variant_20" />

    <!-- Rounded corners -->
    <corners android:radius="20dp" />

    <!-- Border (stroke) -->
    <stroke
        android:width="2dp"
        android:color="@color/primary_dark" />

</shape>
